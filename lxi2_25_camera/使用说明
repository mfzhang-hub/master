####################################################
启动指令

roslaunch lxI2_25_camera lxI2_25_camera.launch
####################################################
发布的topic

rgb图 ${frame_id_base}_rgb
相机内参 ${frame_id_base}_camera_info

其中frame_id_base 在launch文件中指定
######################################################
打开指定相机

在launch文件中指定ip以打开指定相机，如果为空会打开搜索到的第一个相机。
相机的ip会在执行搜索相机时打印到终端中，相机的ip和端口号可以通过调用接口修改。


########################################################
相机默认下发的指令在 param/camera.xml文件中